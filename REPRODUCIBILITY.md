# Reproducibility status

The earlier notebook has not been rerun in a controlled environment. A clean version will need:

1. a verified, openly usable source and licence;
2. a pinned Python environment, including the required spaCy language model;
3. documented cleaning decisions and row counts;
4. an EDA-first notebook whose conclusions are tied to executable outputs.
